const soma = (a, b) => a + b
const subtracao = (a, b) => a - b
const multiplicacao = (a, b) => a * b
const divisao = (a, b) => a / b 

console.log(soma(2, 2))

module.exports = {
    soma,
    subtracao,
    multiplicacao,
    divisao
}